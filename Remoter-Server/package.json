{
  "name": "remoter-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build:web": "npm --prefix ../Remoter-Client run build:web",
    "build": "tsc",
    "build:all": "npm run build:web && npm run build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "@types/ws": "^8.5.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
